apiVersion: apps/v1
kind: Deployment
metadata:
  name: playwright-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: playwright
  template:
    metadata:
      labels:
        app: playwright
    spec:
      containers:
        - name: playwright-container
          image: playwright-automation:latest
